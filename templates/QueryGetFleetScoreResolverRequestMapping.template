{
  "version": "2018-05-29",
  "method": "POST",
  "resourcePath": "/kylin/api/query",
  "params":{
      "body": $util.toJson({
      	"project": "riskos",
        "sql": "select round(sum(scoreAndDistance)/sum(tripDistanceInMetres), 0) from riskos.insurance_transactions_view where fleetId = '$ctx.args.fleetId'"
      }),
      "headers": {
          "Authorization": "Basic $ctx.prev.result.token",
          "Content-Type": "application/json"
      }
  }
}
