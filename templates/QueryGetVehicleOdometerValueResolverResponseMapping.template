#if($ctx.error)
  $utils.error($ctx.error.message, $ctx.error.type)
#end

#set($body = $util.parseJson($ctx.result.body))
#if(!$body || !$body.results)
	$utils.error("Can't parse response.")
#end

$utils.toJson({
	"vin": $body.results[0][0],
    "metres": $body.results[0][1]
})