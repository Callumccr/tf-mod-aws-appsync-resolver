{
    "items": [
        #foreach($entry in $context.result.aggregations.severities.buckets)
                ## $velocityCount starts at 1 and increments with the #foreach loop **
                #if( $velocityCount > 1 ) , #end
                $utils.toJson({
                  "severity": $entry.get('key'),
                  "count": $entry.get('doc_count')
                })
        #end
	]
}